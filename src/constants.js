export const LOADED = 'loaded';
export const RESPONSE = 'response';
export const ERROR = 'error';
export const DEVICE = 'device';
export const LEAVE = 'leave';
export const SYNC = 'sync';
export const POST = 'POST';
export const JOIN_AS_NEW_PEER = 'join-as-new-peer';
export const MUTED = 'muted';

export const DEVICE_LOADED = `${DEVICE}:${LOADED}`;
export const LEAVE_RESPONSE = `${LEAVE}:${RESPONSE}`;
export const JOIN_AS_NEW_PEER_RESPONSE = `${JOIN_AS_NEW_PEER}:${RESPONSE}`;
export const SYNC_RESPONSE = `${SYNC}:${RESPONSE}`;
